// import 'package:fire1/main.dart';
// import 'package:flutter/material(1).dart';

// class DarkMode extends ChangeNotifier {
//   ThemeMode themeMode = ThemeMode.dark;
//   bool get isDarkMode => themeMode == ThemeMode.dark;
//   void toggle(bool isOn) {
//     themeMode = isOn ? ThemeMode.dark : ThemeMode.light;
//     notifyListeners();
//   }
//   // ThemeData _Theme = darkMode ? ThemeData.dark() : ThemeData.light();
//   //GetTheme() {
//   //    return _Theme;
// }

// // SetTheme(ThemeData theme) {
// // _Theme = theme;
// // notifyListeners();
// // }

// //}

// class Mytheme {
//   static final lig = ThemeData(
//       colorScheme: ColorScheme.dark(),
//       scaffoldBackgroundColor: Color.fromARGB(255, 255, 254, 254));
//   static final dar = ThemeData(
//       colorScheme: ColorScheme.light(),
//       scaffoldBackgroundColor: Color.fromARGB(255, 99, 98, 98));
// }
